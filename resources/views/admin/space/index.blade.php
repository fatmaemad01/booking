<x-main-layout title="Spaces">
    {{-- @foreach ($spaces as $space)
        <h4>{{$space->name}}</h4>
        <h4>{{$space->type}}</h4>
        <h4>{{$space->branch->name}}</h4>
        <hr>
    @endforeach --}}

    <h2>Spaces</h2>

</x-main-layout>
